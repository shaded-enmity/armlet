n = 16;
bits(n) a;
bits(n) b;
bits(4) c;

x = 0;
repeat
  a<x> = '1';
  x = x + 1;
until x < n;

x = 0;
while x < n do
  if x MOD 2 == 0 then
    b<x> = 'x';
  x = x + 1;

for I=3 downto 0
  c<I> = '1';

print(a, b, c);
